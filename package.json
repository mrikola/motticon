{
  "private": true,
  "workspaces": ["backend", "frontend"],
  "scripts": {
    "dev": "docker compose up",
    "reset": "docker compose down -v && docker compose build && docker compose up",
    "db:fixtures": "yarn workspace motticon-backend run fixtures",
    "deploy:backend": "yarn workspace motticon-backend deploy",
    "deploy:frontend": "yarn workspace motticon-frontend deploy",
    "deploy:all": "yarn deploy:backend && yarn deploy:frontend"
  }
}
