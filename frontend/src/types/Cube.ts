export type Cube = {
  id: number;
  title: string;
  description: string;
  url: string;
};

export type CubeSelection = {
  key: string;
  value: string;
  displayText: string;
  disabled: boolean;
};
